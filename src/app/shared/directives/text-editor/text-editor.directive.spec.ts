import { TextEditorDirective } from './text-editor.directive';

describe('TextEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorDirective();
    expect(directive).toBeTruthy();
  });
});
